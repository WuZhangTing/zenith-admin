/**
 * 知识中心（Wiki）：空间 / 文档 / 版本 / 模板 / 标签 / 评论 / 统计契约
 *
 * 用法：import { WikiDoc } from '@zenith/shared/wiki'
 * 注意：本入口刻意不导出种子数据，seed 请走 '@zenith/shared/seed'。
 */
export * from './constants';
export * from './types';
export * from './validation';
